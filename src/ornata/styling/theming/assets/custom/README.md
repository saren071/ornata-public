# Custom Themes

Place additional `.osts` theme files in this directory to make them available
for runtime loading through the styling subsystem. Themes must follow the OSTS
syntax described and should declare their color palette inside a `@colors` block.
