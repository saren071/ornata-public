$env:PYTHONUTF8=1
$env:PYTHONIOENCODING="utf-8"
$env:PYTHONPATH = "$(Get-Location)\src"
py -3.14 -m trace --trace test.py > trace.log 2>&1



py -3.14 -m trace --trace --ignore-dir $(python - <<EOF
import sys, os
print(os.path.dirname(sys.executable))
EOF
) test.py > trace.log 2>&1


uv run pytest file_path (-q for quiet) (-v for verbose)



# Delete all *.pyc
Get-ChildItem -Recurse -Filter "*.pyc" -Force | Remove-Item -Force

# Delete all __pycache__ directories
Get-ChildItem -Recurse -Directory -Filter "__pycache__" -Force |
    Remove-Item -Recurse -Force
